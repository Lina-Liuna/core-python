# write a helper function to avoid else blocks after for and while loops

# break the loop as soon as find the target.

# things to remember:
# Avoid using else blocks after loops because their behavior isn't intuitive and can be confusing.
